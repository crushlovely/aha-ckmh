1. Create a vite react app
2. Create a home/index page that runs at the root
3. Create a components folder with a sample component called hero-full-width-card
4. Create a build script so that when this repo is merged to the 'main' branch on github it is deployed to github pages
